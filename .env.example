# Configuration template (REPO-004). Every required configuration key is enumerated here with a
# placeholder or safe-default value and documented with its purpose, type, and default (DOC-005).
# Copy to `.env` and fill values for your environment; `.env` is never committed (REPO-003).
#
# No configuration keys exist yet — application implementation has not started.
# Every key the application requires MUST be added here in the same pull request that
# introduces it (DOC-002), documented as:
#
#   # <purpose> · type: <type> · default: <default or "none — required">
#   KEY_NAME=placeholder
